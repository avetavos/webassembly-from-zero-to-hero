---
title: 'wabt — The Binary Toolkit'
description: 'ใช้ wat2wasm, wasm2wat, wasm-objdump และ wasm-validate เพื่อแปลง ตรวจสอบ และ validate module WebAssembly'
sidebar:
  order: 2
---

import WasmRunner from '../../../../components/WasmRunner.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

นักพัฒนามักหยิบ WABT ขึ้นมาใช้เมื่อต้องแปลงไปมาระหว่าง `.wat` กับ `.wasm`, ตรวจสอบ binary ที่ compile มาจากเครื่องมืออื่นว่าเนื้อในมีอะไรบ้าง หรือ validate module ก่อน deploy จริง

**wabt** — WebAssembly Binary Toolkit — คือ reference implementation ของ WAT text format งานแปลง ตรวจสอบ หรือ validate ใดก็ตามที่เกี่ยวข้องกับ text ↔ binary boundary จะผ่านหนึ่งในสี่โปรแกรมหลักของตัวเอง

## wat2wasm

`wat2wasm` คือ compiler หลัก ทำหน้าที่แยกวิเคราะห์ต้นฉบับ `.wat`, type-check และ emit binary `.wasm` ที่ถูกต้อง flags ที่มีประโยชน์ได้แก่ `--debug-names` (เก็บ annotation `$name` ไว้ใน name section ของ binary) และ `--output` / `-o` เพื่อควบคุมไฟล์ output

```bash
# Basic compilation
wat2wasm add.wat -o add.wasm

# Keep debug names in the binary
wat2wasm add.wat --debug-names -o add.debug.wasm

# Validate only, don't write output
wat2wasm add.wat --no-check=false --output=/dev/null
```

## wasm2wat

`wasm2wat` ทำ process ย้อนกลับ: อ่าน binary `.wasm` และ emit WAT text ที่อ่านได้ สิ่งนี้มีค่ามากเมื่อคุณต้องการตรวจสอบว่า compiler ระดับสูง (Emscripten, wasm-pack ฯลฯ) ผลิตอะไรออกมาจริงๆ

```bash
# Disassemble to stdout
wasm2wat add.wasm

# Disassemble to a file
wasm2wat add.wasm -o add.wat

# Include source locations from the DWARF section
wasm2wat add.wasm --generate-names
```

## wasm-objdump

`wasm-objdump` แสดง metadata ที่มีโครงสร้างเกี่ยวกับไฟล์ `.wasm` — การจัดเรียง section, imports, exports และ function signatures — โดยไม่ต้องสร้าง WAT disassembly ทั้งหมด ใช้ `-x` สำหรับสรุปทั้งหมด หรือ `-d` เพื่อ disassemble เฉพาะ code section

```bash
# Full section summary
wasm-objdump -x add.wasm

# Disassemble code section only
wasm-objdump -d add.wasm

# Show only the export section
wasm-objdump -j export add.wasm
```

ผลลัพธ์ `-x` โดยทั่วไปมีลักษณะดังนี้:

```bash
add.wasm:	file format wasm 0x1

Section Details:

Type[1]:
 - type[0] (i32, i32) -> i32
Function[1]:
 - func[0] sig=0
Export[1]:
 - func[0] <add> -> "add"
Code[1]:
 - func[0] size=7
```

## wasm-validate

`wasm-validate` ตรวจสอบว่า binary เป็นไปตามข้อกำหนด WebAssembly โดยจะออกด้วย code 0 เมื่อสำเร็จและแสดง diagnostic เมื่อล้มเหลว ใช้ใน CI เพื่อตรวจจับ module ที่เสียหายหรือไม่ถูกต้องก่อนที่จะถึง production

```bash
# Validate a module
wasm-validate add.wasm
# → add.wasm: OK

# Validate a module with a future proposal
wasm-validate --enable-threads shared.wasm
```

## Run it — magic bytes และความยาว binary

สี่ไบต์แรกที่ offset 0 ของทุกไฟล์ `.wasm` ที่ถูกต้องคือ `00 61 73 6d` — module magic (`\0asm`) runnable ด้านล่างคอมไพล์ module WAT ขั้นต่ำและแสดงทั้งความยาว binary และสี่ magic bytes เหล่านั้น

export const wabtCode = `const wat = \`(module
  (func (export "add") (param $a i32) (param $b i32) (result i32)
    local.get $a
    local.get $b
    i32.add))\`;
const bytes = await compileWat(wat);
console.log('Binary length:', bytes.length, 'bytes');
const magic = Array.from(bytes.slice(0, 4))
  .map(b => b.toString(16).padStart(2, '0'))
  .join(' ');
console.log('Magic bytes (offset 0):', magic);
console.log('Decoded magic:', String.fromCharCode(...bytes.slice(1, 4)));`;

<WasmRunner client:visible code={wabtCode} />

<Callout title="Magic number ระบุทุกไฟล์ .wasm">
binary WebAssembly ทั้งหมดเริ่มต้นด้วย magic number สี่ไบต์ `00 61 73 6d` ตามด้วย version `01 00 00 00` เครื่องมือหรือ loader ใดก็ตามที่อ่านไฟล์ `.wasm` จะตรวจสอบไบต์เหล่านี้ก่อนเสมอ
</Callout>

## ข้อแลกเปลี่ยน

| ตัวเลือก | Benefit | Cost |
|---|---|---|
| `wasm-opt -Oz` (การ optimise เชิงรุกสุด หลัง `wat2wasm`) | ได้ size และ speed ที่ดีที่สุด | build time นานขึ้น และ binary ที่ได้ห่างไกลจาก `.wat` ต้นฉบับจน debug ยากขึ้น |
| เก็บ `--debug-names` ไว้ตอน `wat2wasm` | `wasm2wat`/`wasm-objdump` แสดงชื่อฟังก์ชันที่อ่านออกได้ | เพิ่มขนาด binary ที่ต้อง ship ให้ผู้ใช้จริง |

## ข้อผิดพลาดที่พบบ่อย

- คอมไพล์ `.wat` เป็น `.wasm` แล้ว ship ตรงๆ โดยไม่ผ่าน `wasm-opt` เลย พลาดการลดขนาดที่ได้มาแทบฟรีๆ
- รัน `wasm-validate` หลังจาก deploy ไปแล้ว แทนที่จะ validate ทุก module ใน CI ก่อน ship เสมอ
- เปิด `--debug-names` ทิ้งไว้ใน production build โดยไม่คิดเรื่อง trade-off ของขนาด binary ที่เพิ่มขึ้น

> 💡 ตัวอย่างจากของจริง
>
> หลังขั้นตอนที่เทียบเท่ากับ `wat2wasm` **Emscripten** เรียก `wasm-opt` ของ Binaryen ให้อัตโนมัติเมื่อ build ด้วย `-O2`/`-O3` ([เอกสาร Emscripten](https://emscripten.org/docs/optimizing/Optimizing-Code.html)) — หลักการ optimize เดียวกันนี้คือสิ่งที่ pipeline ของ WABT ในบทนี้ทำเองด้วยมือ

export const quizWabt = [
  { q: "flag ไหนบอกให้ wat2wasm เก็บ $name annotations ไว้ใน binary ที่ได้?", options: ["--keep-names", "--debug-names", "--names=true", "--preserve-symbols"], answer: 1 },
  { q: "4 bytes แรกของทุกไฟล์ .wasm ที่ valid คืออะไร?", options: ["ff fe 00 00", "00 61 73 6d", "77 61 73 6d", "01 00 00 00"], answer: 1 },
  { q: "wabt tool ไหนที่แสดง section headers และ exports โดยไม่ต้อง disassemble ทั้งหมด?", options: ["wasm2wat", "wasm-validate", "wasm-objdump -x", "wat2wasm --inspect"], answer: 2 },
  { q: "wasm-validate คืน exit code อะไรเมื่อ module ถูกต้อง?", options: ["1", "0", "2", "ขึ้นอยู่กับ OS"], answer: 1 },
];

<Quiz client:visible id="tooling/wabt" questions={quizWabt} />

<ProgressTracker client:visible id="tooling/wabt" />
